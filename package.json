{
  "name": "Io_chat",
  "version": "1.0.0",
  "private": true,
  "description": "Private c_a",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "socket.io": "2.0.3",
    "express": "^4.15.3"
  }
}
